# AI Agent Rules for Obsidian TODO Calendar

このドキュメントは、AIエージェントがObsidianプラグイン（Obsidian TODO Calendar）を介さず、Obsidian Vault 内の Markdown ファイルを直接読み書きしてTODOの検索・作成・更新・完了・削除を行うためのデータ構造仕様および操作ルールです。

---

## 1. ディレクトリ構造 (Directory Structure)

Vault のルート直下にある `_todo-calendar/` ディレクトリ配下でデータを管理します。

```text
<Vault Root>/
└── _todo-calendar/
    ├── templates.json              # タイプ & テンプレートTODO定義ファイル
    ├── collections/
    │   ├── 1724300000000_a1b2c.md
    │   └── 1724300005000_d3e4f.md
    └── items/
        ├── 1724300000000_a1b2c/        # <collection_id> 毎のサブフォルダ
        │   ├── 1724300010000_x1y2z.md  # <item_id>.md
        │   └── 1724300015000_m5n6p.md
        └── 1724300005000_d3e4f/
            └── 1724300020000_q7r8s.md
```

- **`templates.json`**: アクション（Item）に付与できる「タイプ」と、そのタイプに紐づく「テンプレートTODO」の定義を格納。
- **`collections/`**: プロジェクトやシステム単位（コレクション）を定義する Markdown ファイルを格納。
- **`items/<collection_id>/`**: 各コレクションに所属するアイテム（アクション行）を定義する Markdown ファイルを格納。

---

## 2. ID 生成ルール (ID Generation Rule)

- **Collection ID / Item ID**: `Date.now()` (ミリ秒タイムスタンプ) + `_` + 5文字のランダム英数字
  - 例: `1724300000000_a1b2c`
- **TODO ID**: `todo-` + インデックス + `-` + `Date.now()`
  - 例: `todo-0-1724300010000`

---

## 3. ファイルレイアウト & データ構造 (File Format & Data Schema)

### ① テンプレート定義ファイル (`_todo-calendar/templates.json`)

定型業務（システムリリース、見積作成、レビュー等）において、起票漏れや期日未設定を防ぐためのテンプレート定義です。

```json
[
  {
    "id": "release",
    "name": "リリース",
    "color": "blue",
    "icon": "Rocket",
    "templates": [
      {
        "id": "tpl-release-std",
        "name": "通常リリース",
        "todos": [
          { "title": "リリース計画書作成", "group": "計画・申請" },
          { "title": "リリース判定レビュー", "group": "計画・申請" },
          { "title": "変更申請・起票", "group": "計画・申請" },
          { "title": "本番リリース作業", "group": "リリース作業" },
          { "title": "リリース後検証・報告", "group": "リリース作業" }
        ]
      }
    ]
  },
  {
    "id": "estimate",
    "name": "見積",
    "color": "green",
    "icon": "Calculator",
    "templates": [
      {
        "id": "tpl-estimate-std",
        "name": "見積作成・提出",
        "todos": [
          { "title": "要件・工数算出", "group": "見積作成" },
          { "title": "チーム内レビュー", "group": "レビュー" },
          { "title": "部内承認・レビュー", "group": "レビュー" },
          { "title": "顧客提出・提示", "group": "提出" }
        ]
      }
    ]
  }
]
```

### ② コレクションファイル (`collections/<collection_id>.md`)

```markdown
---
id: "1724300000000_a1b2c"
title: "プロジェクト名またはカテゴリ名"
description: "コレクションの概要・説明文"
color: "purple" # （オプション）
created_at: "2026-08-22T06:00:00.000Z"
---
# プロジェクト名またはカテゴリ名

（コレクションに関する自由なメモ書き）
```

### ③ アイテム & TODO ファイル (`items/<collection_id>/<item_id>.md`)

1つのアイテムファイル内に、アイテム自体のステータスと、そのアイテムに所属するTODOリストが Frontmatter の `todos` 配列として含まれます。

```markdown
---
id: "1724300010000_x1y2z"
collection_id: "1724300000000_a1b2c"
title: "2026年9月定期リリース"
type: "release"                 # （オプション）タイプID（例: release, estimate等）
template: "通常リリース"        # （オプション）テンプレート名
status: "todo"                  # "todo" または "done"（アイテム/アクション自体の完了状態）
description: "アイテムの説明や追加メモ"
created_at: "2026-08-22T06:00:00.000Z"
todos:
  - id: "todo-0-1724300010000"
    title: "リリース計画書作成"
    due: "2026-08-22"           # YYYY-MM-DD 形式（期日未設定の場合は空文字 ""）
    status: "todo"              # "todo" または "done"
    description: "タスクの具体的な詳細メモ"
    group: "計画・申請"         # （オプション）グループ名・カテゴリ名
  - id: "todo-1-1724300010000"
    title: "本番リリース作業（夜間対応）" # 前方一致で「本番リリース作業」として充足判定
    due: ""                     # 未設定のためUI上で期日未設定警告が表示される
    status: "todo"
    description: ""
    group: "リリース作業"
---
# 2026年9月定期リリース

（このアイテムに関する詳細仕様や関連リンク等の記述）
```

---

## 4. コンセプトと検証仕様 (Concept & Validation Rules)

### ① タイプとテンプレートの役割
- **コレクション（Collection）**: システム名（例: 基幹システム、ECサイト）や大カテゴリ。
- **アイテム（Item）**: 具体的なアクションやプロジェクト案件（例: 「2026年9月定期リリース」）。
- **タイプ（Type）**: アイテムの種類（例: 「リリース」「見積」）。
- **テンプレート（Template）**: そのタイプで実施すべき定型TODOタスクのセット。

### ② 漏れ防止・警告（Validation & Alert）仕様
アイテムに `type` および `template` が設定されている場合、プラグインUIは以下の2点を自動検証します：

1. **不足TODO（Missing TODOs）**:
   - テンプレートに定義されているタスクが、アイテム内の `todos` に1件も存在しない場合、**「⚠️ 不足TODO: N件」** として警告。
   - **あいまい・前方一致判定**: ユーザーがTODOのタイトルに補足（例: 「本番リリース作業（22:00開始）」）を追記しても、テンプレ名「本番リリース作業」の前方一致・部分一致で充足判定されます。
2. **期日未設定（Missing Due Dates）**:
   - テンプレートTODOはアイテム内に存在するが、`due` が空文字 `""`（日付未設定）の場合、**「⚠️ 期日未設定: M件」** として警告。
   - カレンダー上で日付が確定するまで警告が維持され、計画倒れや日程抜けを防ぎます。

---

## 5. ユーザーの画面表示 & 動線パターン (User View & UX Mental Model)

ユーザーが Obsidian 上でプラグイン UI を開いているとき、データは以下のように視覚化・操作されます。

```text
+-----------------------------------------------------------------------------------------------+
|  [コレクション選択]  ▼ 基幹システム                      [今日のアジェンダ]  [⚙️] [＋新規アイテム] |
+-----------------------------------------------------------------------------------------------+
|  アイテム / 日付             | 2026-08-22 (今日) | 2026-08-23       | 2026-08-24       | ...  |
+------------------------------+-------------------+------------------+------------------+------+
|  [🚀 リリース] 9月定期リリース | [ ] リリース計画   | [ ] 変更申請     |                  |      |
|  ⚠️ 不足: 1件 / 期日未設定: 1件 |                   |                  |                  |      |
+------------------------------+-------------------+------------------+------------------+------+
|  [💰 見積] 新規追加機能      |                   | [ ] 見積提出     |                  |      |
+-----------------------------------------------------------------------------------------------+
```

1. **カレンダーマトリックス (Calendar Matrix View)**:
   - 行ヘッダーに **TypeBadge**（`[🚀 リリース]`）と、検証警告（`⚠️ 不足TODO: 1件` など）を表示。
   - `due` が空文字 `""` のTODOは日付列には表示されず、行ヘッダー警告やドロワーで確認可能。
2. **タスク詳細ドロワー (Task Detail Drawer)**:
   - タイプ・テンプレの切り替えドロップダウン。
   - **TemplateAlertBanner**: 不足TODOリストの表示と「不足TODOを補完（ワンクリック差分追加）」ボタン。
   - TODO一覧で期日が空のタスクに「`[📅 日付未設定 ⚠️]`」バッジを表示。
3. **Template Studio（ヘッダーの ⚙️ アイコン）**:
   - タイプ（名前・色・アイコン）およびテンプレートTODO（タイトル・グループ）のCRUDモーダル。
4. **プライベートVault対応（Feature Toggle）**:
   - Obsidianプラグイン設定で「タイプ & テンプレート機能」をOFFにすると、従来のシンプルなTODOカレンダーとして動作。

---

## 6. AIエージェントの操作ガイドライン (Agent Operational Rules)

### ① タイプ付きアイテムの新規作成とテンプレ展開
1. `_todo-calendar/templates.json` から対象タイプのテンプレTODO定義を読み込みます。
2. `items/<collection_id>/<item_id>.md` を作成し、テンプレTODOを `due: ""`（日付未設定）または指定日付で初期配置します：
   ```yaml
   ---
   id: "1724300010000_x1y2z"
   collection_id: "1724300000000_a1b2c"
   title: "2026年9月定期リリース"
   type: "release"
   template: "通常リリース"
   status: "todo"
   todos:
     - id: "todo-0-1724300010000"
       title: "リリース計画書作成"
       due: "2026-08-22"
       status: "todo"
     - id: "todo-1-1724300010000"
       title: "変更申請・起票"
       due: ""
       status: "todo"
   ---
   ```

### ② 不足TODOの補完
1. 該当アイテムの `todos` と `templates.json` 内の定義を比較。
2. 不足しているTODOがあれば、`due: ""` で `todos` 配列の末尾に追加します。

### ③ TODOの更新・完了化
1. 対象 TODO の `status` を `"done"` または `"todo"` に更新。
2. 日付を確定する場合は `due` を `"YYYY-MM-DD"` 形式で更新。

---

## 7. 編集時の注意事項 & 禁止事項 (Important Constraints)

1. **YAML 構文の厳守**:
   - `todos` 配列や各フィールドのインデント（スペース2つ）とデータ型を守ること。
   - `due` は必ず `"YYYY-MM-DD"` 形式の文字列または空文字列 `""` にすること。
   - `status` は `"todo"` または `"done"` のみ使用すること。
2. **Markdown Body（本文）の保護**:
   - `---` 終了以降の Markdown 本文を誤って上書き消去しないこと。
3. **templates.json の整合性**:
   - JSON形式を破壊しないこと。
4. **文字コード & 改行コード**:
   - UTF-8 で書き出すこと。

